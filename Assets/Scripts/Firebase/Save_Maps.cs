using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Save_Maps : MonoBehaviour
{

    //print the vector data that is being outputted from the random generation algorithm to see what is going on in the 
        //this should then give you more of an insight into how to go about saving that data in realtime and
        //pretty sure map data needs to be in realtime otherwise how else will every player see the same map

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
